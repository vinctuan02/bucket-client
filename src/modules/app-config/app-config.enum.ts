export enum AppConfigKey {
	APP_NAME = 'app_name',
	APP_ICON_URL = 'app_icon_url',
	TRASH_RETENTION_DAYS = 'trash_retention_days',
}

export enum AppConfigFieldMapping {
	KEY = 'key',
	CREATED_AT = 'createdAt',
	UPDATED_AT = 'updatedAt',
}
